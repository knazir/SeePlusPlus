# See++ Backend Configuration for Lambda Execution
# Copy this to .env and update with your values

# Execution mode: local | fargate | lambda
EXEC_MODE=lambda

# Lambda configuration
LAMBDA_FUNCTION_NAME=spp-trace-executor-dev
AWS_REGION=us-west-2

# AWS Credentials (only needed if running locally, not in AWS)
# If running on EC2/ECS/Lambda, these are provided by IAM roles
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Server configuration
PORT=3000
NODE_ENV=development

# CORS configuration
ALLOWED_ORIGIN_REGEX=.*

# File prefix for user code
USER_CODE_FILE_PREFIX=main
